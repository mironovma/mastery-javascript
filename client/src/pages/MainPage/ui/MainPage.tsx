import { memo } from "react";

const MainPage = () => {
    return (
        <div className="py-4">
            <h1>Как повысить свои скиллы</h1>
        </div>
    );
};

export default memo(MainPage);
